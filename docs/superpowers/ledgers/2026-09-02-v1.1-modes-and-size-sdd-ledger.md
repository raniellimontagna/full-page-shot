# SDD ledger — plan: docs/superpowers/plans/2026-09-02-v1.1-modes-and-size.md

Branch: feat/v1.1-modes-and-size (from main 977f096). v1.0.0 verified working in real Chrome by the owner.
Ruling: bounded change on existing flows — compact plan with requirements/interfaces rather than full code, since every hand-written code block in the v1 plan that broke was mine and every implementer that verified instead of transcribing was right. Cost if wrong: implementers make more small judgment calls, each reviewed.

## Task log
Task 1: implementer complete (commit 3e1c03b), 130/130 tests. One out-of-scope touch: mechanical
  DEFAULT_PREFS spread in tests/background/sinks.test.ts to satisfy the widened Prefs type —
  accepted, required to keep typecheck green.
Task 1: review dispatched. Task 2 dispatched in parallel (disjoint files: messages/offscreen/formats
  vs prefs/options). Ruling as in v1: fix rounds for either are also disjoint.
Task 1: review clean — spec ✅, quality approved, zero findings. Upgrade-merge and coercion
  verified load-bearing (one missing coercion would leak into toEqual); alert negative assertion
  still present; sinks.test.ts edit confirmed behaviour-neutral.
Task 1: complete (commits b27f5a7..3e1c03b, review clean)

Task 2: implementer complete (commit 9af15d2), 139/139. Deviation carried to Task 3: capture-loop's
  finishCapture send hardcodes { scale: 2, downloadFormat: 'png', devicePixelRatio: 1 } behind a
  TODO to reproduce 1.0.0 behaviour until Task 3 threads real prefs + dpr and widens runCapture's
  outcome to both URLs. Stitcher.toBlob() replaced by export().
Task 2: review dispatched; Task 3 held until it clears (capture-loop.ts is shared).
Task 2: review clean — spec ✅, quality approved, 0 Critical/Important, 1 cosmetic Minor (a
  speculative comment). planEncode extracted pure and exhaustively tested; stitched canvas never
  mutated; PNG encoded once; hardcoded deviation confirmed byte-for-byte 1.0.0 intent.
Task 2: CARRIED TO TASK 4 e2e: (a) 1x @ dpr2 → PNG exactly half in BOTH axes; (b) PNG download
  and PNG clipboard byte-identical (single-encode contract); (c) jpeg/webp magic bytes on download
  while clipboard stays image/png.
Task 2: complete (commits 3e1c03b..9af15d2, review clean)
Task 3: dispatched (opus — cross-context integration).

Task 3: implementer complete (commit 115a9e7), 156/156. dpr for viewport via executeScript
  func:()=>window.devicePixelRatio (no content script, no new permission). Deviation: captureTab
  keeps a chrome.tabs.Tab argument (e2e hook passes a tab) — behaviour matches brief.
Task 3: CARRIED TO TASK 4: widen the e2e hook (reads only finishCapture replies → viewport reports
  dataUrl null; __fpsSetPrefsForTest takes only the two booleans). "Viewport never injects" is
  structural (ViewportCaptureDeps has no content channel), not unit-tested — e2e must assert it.
Task 3: review dispatched; Task 4 held (e2e hooks live in index.ts).
Task 3: review — spec ✅, quality approved, 1 Important + 2 Minor. Viewport path confirmed
  structurally unable to inject (ViewportCaptureDeps has no content channel); routing test uses
  distinct CLIP/DOWN fixtures so a swap fails; menus idempotent; full-path finally intact.
Task 3: Ruling — folding the "no isTabStillActive on viewport path" Minor into the same fix round
  as the Important. Same file, same failure class (wrong outcome under a confident badge), and the
  viewport path is the one a user will hit most. Cost if wrong: one extra chrome.tabs.get per
  viewport capture.
Task 3: CARRIED TO TASK 4 e2e: viewport observes no hideFixed/measure/scrollTo/restore and no
  content-script executeScript; frames === 1 and elapsed < CAPTURE_INTERVAL_MS; filename ends
  -viewport<ext>; jpeg pref → JPEG on disk, PNG on clipboard. Hook must be widened first
  (__fpsCaptureForTest reads only finishCapture; __fpsSetPrefsForTest takes two booleans;
  probeCapture cannot pass an explicit mode).
Task 3: fix round 1/5 dispatched (resumed original implementer).
Task 3: fix round 1/5 applied (commit 79ec002, 159 tests): dpr probe rejection degrades to dpr 1
  with a warn instead of discarding a good frame; isTabStillActive added to ViewportCaptureDeps.
  Scoped re-review dispatched; Task 4 held (both touch index.ts).
Task 3: fix round 1/5 (2 addressed, 0 open; commits 115a9e7..79ec002). Re-reviewer reproduced the
  red-then-green live. Double catch on dpr judged redundant-but-harmless.
Task 3: minor (deferred): a rejection from isTabStillActive itself is not explicitly tested on the
  viewport path (propagates to the ✕ badge, which is correct).
Task 3: complete (commits 9af15d2..79ec002, review clean)
Task 4: dispatched (opus — e2e + spec + docs + release).

Task 4: implementer complete (commits bce28f7, 60763f1, 9910bf4; tag v1.1.0 local). Gates from
  clean tree: oxlint / typecheck / 159 unit / build / 24 e2e (17 + 7). Brief assertion contradicted
  by real Chromium ("viewport under one CAPTURE_INTERVAL_MS"): first offscreen creation costs ~1s.
  Implementer measured both modes on the same page and asserts viewport < full/3 (1.07s vs 4.9s),
  full run doubling as the control. Accepted — stronger than the absolute bound, not weaker.
  fractional-dpi.spec.ts now pins scale: 2 explicitly since it tests device-pixel rows.
Task 4: Ruling — merging Task 4's task review INTO the final whole-branch review. The branch is
  four tasks; the final reviewer reads the whole diff anyway, and Task 4 is mostly tests/docs. The
  final dispatch requires a separate verdict section for Task 4 so nothing is skipped. Cost if
  wrong: one fewer review seat on a docs+tests task, offset by the most capable model reading it.
=== ALL 4 TASKS COMPLETE. Final whole-branch review: MERGE_BASE 977f096 .. HEAD 9910bf4 ===
Final review: first dispatch killed by the account session rate limit before reading anything —
  no partial work, no state to reconcile. Re-dispatched identically after the reset.

FINAL REVIEW (977f096..9910bf4): all five gates green on the exact tree (159 unit, 24 e2e).
  0 Critical, 1 Important, 4 Minor. Task 4 verdict: spec ✅, approved with one minor fix.
  Deferred Task 3 minor: DROP (rejection path already produces the same ✕ outcome; nothing to
  restore on the viewport path).
Final: Ruling — the Important is the FOURTH test in this project that cannot fail for the reason it
  claims: `toBe` on two PNG data URLs proves format routing, not single-encode, because PNG
  encoding is deterministic. Fix = keep the e2e assertion but re-label its contract, and add a unit
  test on exportBoth asserting Stitcher.export is called exactly once for png. Cost if wrong: none.
Final: Ruling — timing threshold viewport < full/3 relaxed to full/2. The cold offscreen creation
  (~1s) lands on the viewport run; on a slow CI box the ratio degrades in the flaky direction while
  the full run is dominated by fixed 550ms spacing. Cost if wrong: a slightly weaker speed claim.
Final: fix wave dispatched (1 Important + 4 Minor, all small).
Final fix wave: complete (commit 09925a5; tag v1.1.0 moved; zip rebuilt). Gates: oxlint / typecheck
  / 161 unit / build / 24 e2e. Red-then-green on the new exportBoth unit test (`expected 1, got 2`
  when sabotaged). Scoped re-review dispatched.
Final: scoped re-review clean — all 5 ADDRESSED, red-then-green reproduced independently, no new
  breakage, merge-ready. Deferred Task 3 minor: dropped per final review.
=== RUN COMPLETE: 4 tasks, 9 commits ahead of main, 161 unit + 24 e2e, v1.1.0 tagged locally. ===
