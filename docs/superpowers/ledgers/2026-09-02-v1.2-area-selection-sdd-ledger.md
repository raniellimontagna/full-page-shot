# SDD ledger — plan: docs/superpowers/plans/2026-09-02-v1.2-area-selection.md

Branch: feat/v1.2-area-selection (from main 61e1730 = v1.1.0). Same method as v1.1: compact plan, requirements + interfaces, no hand-written code.
Ruling: Tasks 2 (content overlay) and 3 (offscreen crop) are independent of each other and both depend only on Task 1 types — they will run in parallel after Task 1 clears. Cost if wrong: a merge conflict in messages.ts, which Task 1 owns and freezes first.

## Task log

Task 1: implementer complete (commit 60fabc1), 169/169. Placeholder `selectArea` case in
  content/index.ts returns not-implemented and sits AFTER the abandoned-capture guard — Task 2 must
  replace the whole case. CssRect lives in messages.ts.
Task 1: review dispatched. Tasks 2 and 3 dispatched in parallel (both read messages.ts, neither
  modifies it; Task 2 owns src/content, Task 3 owns src/offscreen + src/shared/crop.ts).
Task 1: review clean — spec ✅, quality approved, 0 Critical/Important, nothing blocks parallel
  work. Frozen types confirmed unambiguous (undefined / null / CssRect three-state matches the
  existing `measurements?` convention).
Task 1: minor (deferred): MIN_SELECTION_PX doc comment is `//` not JSDoc — cosmetic.
Task 1: complete (commits f22629f..60fabc1, review clean)

Task 2: implementer complete (commit e1d6bb1), 203/203 (+18). No PointerEvent polyfill needed;
  jsdom 30 lacks setPointerCapture so move/up listen on the document (also better in real
  browsers). Inline !important host styles. selectArea above the guard; restorePage removes the
  overlay. Concerns to judge in review: (a) a drag > RESTORE_WATCHDOG_MS (10s) is cancelled by the
  watchdog — a user deliberating loses the overlay; (b) transform/filter on html/body breaks
  position: fixed for the host (CSS trap, document as limitation).
Task 2: review dispatched (opus).
Task 3: implementer complete (commit fed5935), 210/210 (+23). planCrop rounds edges independently
  so adjacent selections tile; crop path decodes once and draws directly (does not reuse
  stitcherFromFrame, whose crop param is exercised by its own unit test). Review dispatched.
Task 4: Ruling — dispatching while Tasks 2 and 3 are still under review. Task 4 owns src/background,
  src/manifest.config.ts, tests/background, tests/manifest.test.ts; any Task 2/3 fix round lands in
  src/content or src/offscreen + src/shared/crop.ts. Disjoint. Task 4 consumes only the Task 1
  frozen types (approved) — it passes `crop` through a message, it never imports planCrop. Cost if
  wrong: a hand-resolved merge conflict.
Task 3: review — spec ✅, quality approved, 1 Important (stitcherFromFrame's crop param dead in
  production; two draw paths for one behaviour) + 1 Minor (no dpr-2 adjacency case). Rounding,
  clamp, decode-once, close-in-finally, crop-before-export all verified by hand.
Task 3: Ruling — collapsing to one path by moving planCrop INSIDE stitcherFromFrame (takes CSS rect
  + dpr, plans after its own decode). DeviceRect goes internal. Cost if wrong: one extra parameter
  object on an internal function.
Task 3: fix round 1/5 dispatched (resumed original implementer).
Task 2: review — spec ✅, quality NOT approved: 3 Important + 2 Minor. All exit paths traced to
  the same finally; guard placement correct; transform/filter containing-block trap confirmed
  unfixable (removing the ancestor transform would itself alter the page) — document as limitation.
  Important: (1) watchdog armed once, never re-armed — a 10s deliberation yanks the overlay;
  (2) the two-rAF wait is NOT load-bearing — deleting both awaits leaves 47/47 green — the FIFTH
  test in this project that cannot fail for its stated reason; (3) Escape/pointer events leak to
  the page (no stopPropagation) — a cancel that alters the page. Minor: rAF never fires in a hidden
  tab → selectArea hangs in finally (race with a 200ms timeout, shared helper with scroll-driver);
  wheel scroll not blocked during drag.
Task 2: Ruling — folding both Minors into the fix round: the hidden-tab hang breaks the reply
  guarantee (watchdog cannot rescue a settled-but-hanging finally), and the wheel fix is one
  listener. Cost if wrong: two small extra listeners.
Task 2: fix round 1/5 dispatched (resumed original implementer).
Task 2: CARRIED TO TASK 5 e2e: (a) sample four pixels just outside the selection in the decoded
  PNG — must match the undimmed page colour, not 35%-darkened (jsdom can never settle the rAF
  guarantee); (b) page-level keydown counter stays 0 after an Escape cancel; (c) idle 11s then drag
  still yields a rect; a genuinely abandoned overlay is gone with scrollY unchanged.
Task 3: fix round 1/5 applied (commit d0601e4): single crop path via stitcherFromFrame(dataUrl,
  { rect, devicePixelRatio }), DeviceRect internal, planCrop-throw + dpr-2 adjacency tests. Scoped
  re-review dispatched. (Transient tests/background failure seen by the implementer = Task 4's
  in-progress work, not this diff.)
Task 3: fix round 1/5 (2 addressed, 0 open; commits fed5935..d0601e4). Single production call site
  for stitcherFromFrame with crop; planCrop-throw test proven to fail without the finally/close.
Task 3: minor (deferred): dpr-2 adjacency case covers the x-axis only (y-axis uses the identical
  formula). Transient typecheck failures in tests/background and tests/content are the concurrent
  Task 4 / Task 2 fix agents' in-progress work, not this diff.
Task 3: complete (commits 60fabc1..d0601e4 [offscreen commits], review clean)

Task 4: implementer complete (commit f6923cb). 180 node-project tests + 10 options green for its
  files; whole-suite red only because the Task 2 fix agent has uncommitted work in src/content.
Task 4: Ruling — MY design error: Ctrl+Shift+I / Command+Shift+I is Chrome's DevTools shortcut, and
  browser-level bindings beat extension commands, so the selection command would never fire.
  Switching to Ctrl+Shift+S / Command+Shift+S (unbound in Chrome on all platforms; "S for
  selection"). Folded into Task 4's fix round; plan amended. Cost if wrong: a different key to
  remember — the menu item and options default still work regardless.
Task 4: CARRIED TO TASK 5: the e2e probe must START the capture without awaiting (captureTab now
  blocks on the drag), drive page.mouse, THEN await — or it hangs until the 10s watchdog cancels.
  Single-flight is held for the whole drag; a second click during selection shows the busy badge.
Task 4: review dispatched (opus).
Task 2: fix round 1/5 applied (commit 53074fd, 250 tests): watchdog re-armed on activity (above the
  drag guard — hovering while deciding counts); two-rAF test mutation-proved both ways; all four
  listeners capture-phase on the document with stopImmediatePropagation; nextFrame raced with a
  200ms timeout via a shared helper also used by scroll-driver; wheel blocked. Scoped re-review
  dispatched. Note: FRAME_TIMEOUT_MS = 200 now also bounds the scroll driver — Task 5's e2e is
  where a late frame would surface.
Task 2: fix round 1/5 (5 addressed, 0 open; commits e1d6bb1..53074fd). Re-reviewer reproduced both
  mutations itself (delete both awaits → 2 fail; remove stopImmediatePropagation → counter test
  fails). Deviation (reportActivity above the drag guard) judged sound and effectively required.
Task 2: minor (deferred): FRAME_TIMEOUT_MS = 200 now bounds scroll-driver too — a frame later than
  200ms is no longer awaited; SETTLE_DELAY_MS partially compensates; hanging forever in a hidden tab
  was strictly worse. Task 5 e2e is where a late frame would surface.
Task 2: complete (commits 60fabc1..53074fd [content commits], review clean)

Task 4: review — spec ✅, quality approved, 1 Important (cancel→neutral wiring untested: swapping in
  BADGE_FAILURE leaves 148 tests green) + 2 Minor (integer RECT fixture hides worker-side rounding;
  near-vacuous _execute_action test). Reviewer endorses holding single-flight for the whole drag:
  releasing early would photograph the overlay into a second capture.
Task 4: fix round 1/5 dispatched (resumed original implementer) — includes the Ctrl+Shift+S move.
Task 4: CARRIED TO TASK 5 e2e (verbatim from the reviewer): stash the pending probe promise in
  worker.evaluate, drive page.mouse, then await it — never await first (hangs until the 10s
  watchdog), never run two probes concurrently; cancel → badge '·', frames 0, downloadRequests 0,
  clipboardDataUrl null, contentMessages exactly ['selectArea']; crop → decoded PNG
  round(300*dpr) × round(200*dpr) at scale 1 (±1), identical for the reverse drag; page unaltered
  (scrollY, no fps-selection-overlay) after both drag and Esc; read `badge` straight off the
  resolved probe (neutral window is 1.5s).
Task 4: fix round 1/5 applied (commit 35f6c84, 253 tests): Ctrl+Shift+S chord; cancel→neutral and
  onClicked-uses-pref assertions each fail exactly the intended tests under mutation; fractional RECT
  fixture now fails under worker-side rounding. Scoped re-review dispatched.
Task 5: Ruling — dispatching while Task 4's scoped re-review runs. Task 5 owns e2e/**, docs, versions,
  zip, and ONLY the build-gated e2e hook block inside src/background/index.ts; a Task 4 re-review fix
  would land in mode-dispatch/manifest/tests, not the hook block. Task 5 is the longest task; the
  re-review is minutes. Cost if wrong: a hand-resolved conflict in index.ts.
Task 5: dispatched (opus).
Task 4: fix round 1/5 (4 addressed, 0 open; commits f6923cb..35f6c84). Re-reviewer reproduced both
  mutations itself. Report inaccuracy noted: "grep Shift+I empty" is false — 5 hits remain, all in
  comments and the negative-assertion test; harmless.
Task 4: complete (commits d0601e4..35f6c84 [background commits], review clean)

Task 5: implementer complete (commits 8b914ce, 981d73e, c8a457d; tag v1.2.0 local). Gates from a
  clean tree: oxlint / typecheck / 253 unit / build / 31 e2e. Four falsifiability mutations proved
  in two Chromium runs. Two deviations, both sound: (a) "pixels just outside the selection" are not
  observable in a cropped image → four corner pixels inside + whole-image colour census, with a
  mid-drag raw frame as the non-vacuity control; (b) "idle 11s with the mouse still" IS abandonment
  by design → the deliberation test hovers for 11s, which is what exercises the re-arm. Test 3 takes
  two frames (control + real). Colour census verified at dpr 1 only.
Task 5: Ruling — as in v1.1, Task 5's task review is folded into the final whole-branch review with a
  dedicated verdict section. Cost if wrong: one fewer seat on a tests+docs task.
=== ALL 5 TASKS COMPLETE. Final whole-branch review: MERGE_BASE 61e1730 .. HEAD c8a457d ===

FINAL REVIEW (61e1730..c8a457d): all five gates green on the exact tree (253 unit, 31 e2e). 0 Critical,
  1 Important, 5 Minor. Task 5 verdict: spec ✅, approved with follow-ups; both deviations judged
  sound and strictly stronger than the brief. No sixth vacuous test found — the reviewer probed the
  highest-risk candidates. Deferred triage: 2 drop, 1 follow-up (scroll-driver no-frame test).
Final: Ruling — Important #1 (second selectArea's session #1 finally clears the watchdog protecting
  overlay #2) is a genuine hole in the one absolute rule, reachable across a worker restart. Fix by
  generation token: `const gen = ++selectionGen` before armWatchdog(), `if (gen === selectionGen)
  clearWatchdog()` in finally. Cost if wrong: none — five lines.
Final: Ruling — one fix wave: Important #1 + Minors #2 (wheel listener to document, capture phase),
  #4 (stale three-path comment), #5 (decodeFrame export), #6 (assert the return), #3 (run the
  selection suite's census assertions at deviceScaleFactor 2 — the helper already supports it), plus
  the deferred follow-up (scrollToStep no-frame test). All small; none change behaviour except #2.
Final: fix wave dispatched.
Final fix wave: complete (commits 4de2b4a, 2f1fe65; tag v1.2.0 moved; zip rebuilt). Gates: oxlint /
  typecheck / 254 unit / build / 32 e2e. dpr-2 census test passed as delivered. Scoped re-review
  dispatched.
Final: scoped re-review clean — 7/7 ADDRESSED, mutation reproduced live, 32/32 e2e, no new breakage,
  merge-ready. Token reasoning: a third concurrent selectArea and a post-restore selectArea are both
  safe (monotonic counter; restore never touches selectionGen).
=== RUN COMPLETE: 5 tasks, 14 commits ahead of main, 254 unit + 32 e2e, v1.2.0 tagged locally. ===
